name: Deploy Vite Project to GitHub Pages

on:
  push:
    branches:
      - main # یا هر برنچی که کد اصلی شما روی آن است

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies 📦
        run: npm ci

      # ✅ دستور بیلد صحیح برای پروژه شما این است
      - name: Build 🏗️
        run: npm run build

      - name: Deploy 🚀
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # ✅ پوشه بیلد پروژه شما dist است
          publish_dir: ./dist
